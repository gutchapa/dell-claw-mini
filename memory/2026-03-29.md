
- **16:36 UTC**: Upgraded `smart_router.mjs` to directly use the local Ollama API (specifically `tinydolphin` and `deepseek-r1:1.5b`) instead of the slow `node-llama-cpp` dependency. Achieved sub-500ms triage time for local intents (e.g., muting the Sony TV).
